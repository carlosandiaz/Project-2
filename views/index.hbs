<h1 class="main-title">{{title}}</h1>

<h2 class="page-header">Books Recently Added</h2>
<div class="book_list">
  {{#each books}}
  <a href="/books/{{id}}">
    <img height="250" width="200" src="{{coverImagePath}}">
  </a>
  {{/each}}
</div>